server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rolepaly?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: escape546
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

fastapi:
  base-url: http://localhost:8000
